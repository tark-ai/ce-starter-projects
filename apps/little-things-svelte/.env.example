# Commerce Engine storefront credentials (exposed to the client via the PUBLIC_ prefix)
PUBLIC_STORE_ID=your_store_id
PUBLIC_API_KEY=your_api_key
# "staging" (default) or "production"
PUBLIC_CE_ENV=staging
